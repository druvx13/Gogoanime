<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
    <link rel="shortcut icon" href="{{ BASE_URL }}/img/favicon.ico">

    <title>List All Anime at {{ WEBSITE_NAME }} | Anime List</title>

    <meta name="robots" content="index, follow" />
    <meta name="description" content="List All Anime at {{ WEBSITE_NAME }} | Anime List">
    <meta name="keywords" content="List All Anime at {{ WEBSITE_NAME }} | Anime List">
    <meta itemprop="image" content="{{ BASE_URL }}/img/logo.png" />

    <meta property="og:site_name" content="{{ WEBSITE_NAME }}" />
    <meta property="og:locale" content="en_US" />
    <meta property="og:type" content="website" />
    <meta property="og:title" content="List All Anime at {{ WEBSITE_NAME }} | Anime List" />
    <meta property="og:description" content="List All Anime at {{ WEBSITE_NAME }} | Anime List">
    <meta property="og:url" content="" /> {# Or pass current URL #}
    <meta property="og:image" content="{{ BASE_URL }}/img/logo.png" />
    <meta property="og:image:secure_url" content="{{ BASE_URL }}/img/logo.png" />

    <meta property="twitter:card" content="summary" />
    <meta property="twitter:title" content="List All Anime at {{ WEBSITE_NAME }} | Anime List" />
    <meta property="twitter:description" content="List All Anime at {{ WEBSITE_NAME }} | Anime List" />

    <link rel="canonical" href="{{ BASE_URL }}{{ request_uri }}" />
    <link rel="alternate" hreflang="en-us" href="{{ BASE_URL }}{{ request_uri }}" />
    <link rel="stylesheet" type="text/css" href="{{ BASE_URL }}/css/style.css" />
    <script type="text/javascript" src="{{ BASE_URL }}/js/libraries/jquery.js"></script>
    {% include 'parts/popup.html.twig' %}
    <script>
        var base_url = 'https://' + document.domain + '/'; // Or use {{ BASE_URL }}
        var base_url_cdn_api = 'https://ajax.gogocdn.net/';
        var api_anclytic = 'https://ajax.gogocdn.net/anclytic-ajax.html';
    </script>
    <script type="text/javascript" src="https://cdn.gogocdn.net/files/gogo/js/main.js?v=7.1"></script>
</head>

<body>
    <div class="clr"></div>
    <div id="wrapper_inside">
        <div id="wrapper">
            <div id="wrapper_bg">
                {% include 'layout/header.html.twig' %}
                <section class="content">
                    <section class="content_left">
                        <div class="main_body">
                            <div class="anime_name anime_list">
                                <i class="icongec-anime_list i_pos"></i>
                                <h2>ANIME LIST</h2>
                                <div class="anime_name_pagination">
                                    <div class="pagination">
                                        {{ pagination_html|raw }}
                                    </div>
                                </div>
                            </div>
                            <div class="list_search">
                                <ul>
                                    <li class="first-char"><a href="/anime-list.html" rel="all" class="{{ active_char == 'All' ? 'active' : '' }}">All</a></li>
                                    {% for char_code in 65..90 %} {# A to Z #}
                                        {% set char = char_code|abs|chr %}
                                        <li class="first-char"><a href="/anime-list-{{ char }}" rel="" class="{{ active_char == char ? 'active' : '' }}">{{ char }}</a></li>
                                    {% endfor %}
                                </ul>
                            </div>
                            <div class="anime_list_body">
                                <ul class="listing">
                                    {% for anime in anime_list_items %}
                                    <li title="{{ anime.liTitle|escape('html_attr') }}"> <a href="/category/{{ anime.animeId }}" title="{{ anime.animeTitle|escape('html_attr') }}">{{ anime.animeTitle }}</a></li>
                                    {% else %}
                                    <li>No anime found for this letter/page.</li>
                                    {% endfor %}
                                </ul>
                                <div class="clr"></div>
                            </div>
                        </div>
                    </section>
                    <section class="content_right">
                        <div class="headnav_center"></div>
                        <div class="clr"></div>
                        <div class="main_body">
                            <div class="main_body_black">
                                <div class="anime_name ongoing">
                                    <i class="icongec-ongoing i_pos"></i>
                                    <h2>RECENT RELEASE</h2>
                                </div>
                                <div class="recent">
                                    <div id="scrollbar2">
                                        <div class="scrollbar"><div class="track"><div class="thumb"><div class="end"></div></div></div></div>
                                        <div class="viewport">
                                            <div class="overview">
                                                {{ recent_release_content|raw }}
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="clr"></div>
                        <div id="load_ads_2">
                            <div id="media.net sticky ad" style="display: inline-block"></div>
                        </div>
                        <style type="text/css">
                            #load_ads_2 { width: 300px; }
                            #load_ads_2.sticky { position: fixed; top: 0; }
                            #scrollbar2 .viewport { height: 1000px !important; }
                        </style>
                        <script>
                            // JS for sticky ad - consider moving to a global JS file if used on multiple pages
                            var leftamt;
                            function scrollFunction() {
                                var scamt = (document.documentElement.scrollTop ? document.documentElement.scrollTop : document.body.scrollTop);
                                var element = document.getElementById("media.net sticky ad");
                                if (element && typeof leftamt !== 'undefined' && scamt > leftamt) { // Added null check for element
                                    var leftPosition = element.getBoundingClientRect().left;
                                    element.className = element.className.replace(/(?:^|\s)fixclass(?!\S)/g, '');
                                    element.className += " fixclass";
                                    element.style.left = leftPosition + 'px';
                                } else if (element) {
                                    element.className = element.className.replace(/(?:^|\s)fixclass(?!\S)/g, '');
                                }
                            }
                            function getElementTopLeft(id) {
                                var ele = document.getElementById(id);
                                if (!ele) return { top: 0, left: 0 }; // Added null check
                                var top = 0;
                                var left = 0;
                                while (ele.tagName != "BODY") {
                                    top += ele.offsetTop;
                                    left += ele.offsetLeft;
                                    ele = ele.offsetParent;
                                    if (!ele) break; // Break if no offsetParent
                                }
                                return { top: top, left: left };
                            }
                            function abcd() {
                                var adElement = document.getElementById("media.net sticky ad");
                                if (adElement) { // Check if element exists
                                    TopLeft = getElementTopLeft("media.net sticky ad");
                                    leftamt = TopLeft.top;
                                    //leftamt -= 10;
                                }
                            }
                            window.onload = abcd;
                            window.onscroll = scrollFunction;
                        </script>
                        {% include 'parts/sub-category.html.twig' %}
                    </section>
                </section>
                <div class="clr"></div>
                {% include 'layout/footer.html.twig' %}
            </div>
        </div>
    </div>
    <div id="off_light"></div>
    <div class="clr"></div>
    <div class="mask"></div>
    <script type="text/javascript" src="{{ BASE_URL }}/js/files/combo.js"></script>
    <script type="text/javascript" src="{{ BASE_URL }}/js/files/video.js"></script> {# Assuming this is needed #}
    <script type="text/javascript" src="{{ BASE_URL }}/js/files/jquery.tinyscrollbar.min.js"></script>
    <script type="text/javascript" src="{{ BASE_URL }}/js/files/jqueryTooltip.js"></script>
    <script type="text/javascript">
        $(".listing li[title]").tooltip({ offset: [10, 200], effect: 'slide', predelay: 300 }).dynamic({ bottom: { direction: 'down', bounce: true } });
    </script>
    <style type="text/css">
        .hide { display: none; }
        .anime_list_body, .anime_list_body ul {
            width: 100%;
            box-sizing: border-box;
        }
        .anime_list_body ul li, .anime_list_body ul li a {
            white-space: nowrap;
            overflow: hidden;
            padding-right: 10px;
            display: block;
        }
        .anime_list_body { padding: 14px 18px; }
        .anime_list_body ul li a { line-height: 115%; }
    </style>
    <script>
        if (document.getElementById('scrollbar2')) {
            $('#scrollbar2').tinyscrollbar();
        }
    </script>
</body>
</html>
